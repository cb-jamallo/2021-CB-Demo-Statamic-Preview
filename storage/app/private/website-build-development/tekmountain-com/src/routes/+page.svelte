<script>'use strict'; /* SVELTE/KIT IMPORTS */import { tick, onMount, afterUpdate } from 'svelte'; /* CUSTOM IMPORTS */import Store from '$lib/components/_stores/store'; import { StoreEnvVars } from "$lib/components/_stores/store-env-vars"; import { TemplateParseTitle } from "$lib/components/_stores/template-functions"; /* CUSTOM JS */ let swiperInitWhatDrivesUs = ( _swiper ) => {console.log( _swiper.activeIndex ) const swiperSlideDetails = document.querySelectorAll('.what-drives-us-detail');const swiperSlideDetailHide = () =>{swiperSlideDetails.forEach( ( _item, _itemIndex ) => {_item.style.display = 'none';_item.style.opacity = 0; }); } const swiperSlideDetailShow = ( _i ) => {swiperSlideDetails.forEach( ( _item, _itemIndex ) => { _item.style.display = 'none'; _item.style.opacity = 0; if ( _itemIndex + 1 === _swiper.activeIndex || _itemIndex + 1 === 1 && _swiper.activeIndex> swiperSlideDetails.length || ( _itemIndex + 1 === swiperSlideDetails.length && _swiper.activeIndex === 0 )) {_item.style.display = 'initial';_item.style.opacity = 1; } }); } _swiper.on('slideChange', function ( _slide ) {console.log('slide changed', _swiper.activeIndex ); swiperSlideDetailShow( _swiper.activeIndex ); }); swiperSlideDetailHide(); swiperSlideDetailShow(1);} onMount(async () => { tick();}); afterUpdate(async () => {tick();/*let temp = setInterval( () => {if ( window.swiperInstances ); clearInterval(temp); swiperInitWhatDrivesUs( window.swiperInstances[0] );}, 10);*/ }); </script><svelte:head> <title>{ TemplateParseTitle( `TekMountain` ) }</title> <meta name='description' content='...' /></svelte:head>